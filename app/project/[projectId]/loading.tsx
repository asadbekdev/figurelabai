import { FigureLabLoading } from "@/components/product/app-screens"

export default function Loading() {
  return (
    <FigureLabLoading
      title="Loading project"
      description="Restoring the stable editor shell and project state."
    />
  )
}
