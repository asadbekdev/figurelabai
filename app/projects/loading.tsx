import { FigureLabLoading } from "@/components/product/app-screens"

export default function Loading() {
  return (
    <FigureLabLoading
      title="Loading projects"
      description="Preparing the project index and saved figures."
    />
  )
}
