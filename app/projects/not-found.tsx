import { FigureLabNotFound } from "@/components/product/app-screens"

export default function NotFound() {
  return (
    <FigureLabNotFound
      title="Projects not found"
      description="That project index does not exist or has not been created yet."
    />
  )
}
